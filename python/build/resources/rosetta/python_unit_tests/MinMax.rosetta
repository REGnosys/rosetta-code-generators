namespace rosetta_dsl.test.semantic.min_max : <"generate Python unit tests from Rosetta.">

type MinTest:
    a int (1..1)
    condition Test:
        if [a,1]min = 1
           then True
        else
           False

type MaxTest:
    a int (1..1)
    condition Test:
        if [a,1,20]max = 20
           then True
        else
           False